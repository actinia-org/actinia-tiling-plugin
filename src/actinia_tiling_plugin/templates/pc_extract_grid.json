{
  "list": [
    {% for cat in range(1, n + 1) %}
    {
      "id": "",
      "module": "v.extract",
      "inputs": [
        {
          "param": "input",
          "value": "{{ grid_name }}"
        },
        {
          "param": "cat",
          "value": "{{ cat }}"
        }
      ],
      "outputs": [
        {
          "param": "output",
          "value": "{{ grid_prefix }}{{ cat }}"
        }
      ],
      "superquiet": true
    }{{ "," if not loop.last }}
    {% endfor %}
  ],
  "version": "1"
}
